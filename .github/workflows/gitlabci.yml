# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4 
      
      - name: Test
        run: echo "Helo World"

      - name: install packages
        run: |
          sudo apt install git
          sudo apt install ansible

      - name: get Repo
        run: |
          git clone https://github.com/Salzstange-app/Homeflix.git

     
  tests:
    runs-on: ubuntu-latest   

    steps:
      - uses: actions/checkout@v4

      - name: Start Bash Script
        run: bash .sh

      - name: Check Sonarr
        run: |
          curl --fail http://localhost:8989/api/v1/system/status

      - name: Check Radarr
        run: |
          curl --fail http://localhost:7878/api/v1/system/status

      - name: Check Sabnzbd
        run: |
          curl --fail http://localhost:8080/api/v2/sabnzbd/pause

      - name: Check Jellyfin
        run: |
          curl --fail http://localhost:8096/Users